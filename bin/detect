#!/usr/bin/env bash
# bin/detect <build-dir>

if [ -f $1/Frontend/checkout-price-calculator/package.json ]; then
  cd Frontend/checkout-price-calculator && echo "React.js (create-react-app) multi" && exit 0
else
  echo "no" && exit 1
fi
